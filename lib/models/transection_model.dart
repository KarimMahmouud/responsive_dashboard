class TransactionModel {
  final String title, suntitle, amount;
  final bool color;

  const TransactionModel({
    required this.title,
    required this.suntitle,
    required this.amount,
    required this.color,
  });
}
